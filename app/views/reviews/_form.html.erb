<h3>Ajouter un commentaire</h3>
<%= simple_form_for([restaurant, review]) do |f| %>
<%= f.input :content %>
<%= f.input :rating, collection:[1,2,3,4,5], as: :radio_buttons %>
<%= f.submit class: "btn btn-primary" %>
<% end %>
